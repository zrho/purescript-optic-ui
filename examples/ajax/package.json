{
  "private": true,
  "scripts": {
    "postinstall": "pulp dep install",
    "build": "mkdir -p output && pulp browserify > output/app.js"
  },
  "devDependencies": {
    "pulp": "^4.0.1",
    "rimraf": "^2.4.1"
  }
}
